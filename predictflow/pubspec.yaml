name: predictflow
description: Pure Dart prediction market engine and HTTP server replacing the old Node/TypeScript project.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
